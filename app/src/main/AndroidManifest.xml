<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Amply">

        <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="AIzaSyCyHbrOvJEmfWm9mB0d16HMHkbDt_XOlvI" />

        <!-- Launcher Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="true" />

        <!-- Dashboard Activities -->

        <activity android:name=".ui.dashboard.EvOperatorDashboard" android:exported="true" />
        <activity android:name=".ui.dashboard.EvOwnerDashboard" android:exported="true" />

        <!-- Reservation Activities -->
        <activity android:name=".ui.reservation.ReservationListActivity" android:exported="true" />
        <activity
            android:name=".ui.reservation.MyReservationsActivity"
            android:exported="false"
            android:theme="@style/Theme.Amply" />
        <!-- EV Operator Module Activities -->
        <activity
            android:name=".ui.EVOperatorApp.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.EVOperatorApp.QRScannerActivity"
            android:exported="true" />

        <activity
            android:name=".ui.EVOperatorApp.BookingDetailsActivity"
            android:exported="true" />

        <activity
            android:name=".ui.EVOperatorApp.FinalizeOperationActivity"
            android:exported="true" />

        <!-- Add this line for AccountActivity -->
        <activity android:name=".ui.dashboard.EvOperatorAppBar.AccountActivity"
            android:exported="true" />

    </application>

</manifest>